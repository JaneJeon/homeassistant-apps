#include <tunables/global>

profile grafana-image-renderer flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  # Capabilities
  file,
  network,
  signal (send) set=(kill,term,int,hup,cont),
  signal (receive),

  # Grafana Image Renderer binary
  /usr/bin/grafana-image-renderer rix,

  # Tini init
  /usr/bin/tini rix,

  # Chromium browser
  /usr/bin/chromium rix,
  /usr/lib/chromium/** rix,

  # Shell and utilities
  /bin/** rix,
  /usr/bin/** rix,
  /usr/lib/** rm,

  # Shared libraries
  /lib/** rm,
  /lib64/** rm,

  # Config and options
  /data/** rw,
  /run.sh rix,

  # Temporary files (Chromium rendering)
  /tmp/** rwk,
  /dev/shm/** rw,

  # Font access
  /usr/share/fonts/** r,
  /etc/fonts/** r,
  /var/cache/fontconfig/** r,

  # Locale and SSL
  /usr/lib/locale/** r,
  /etc/ssl/** r,
  /usr/share/ca-certificates/** r,

  # Proc filesystem (Chromium needs this)
  @{PROC}/** r,

  # Device access
  /dev/tty rw,
  /dev/null rw,
  /dev/urandom r,

  # Home directory for nonroot user
  /home/nonroot/** rw,
}
